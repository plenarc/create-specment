# create-specment プロジェクトガイドライン

## バージョン管理ルール

main へ PR してマージする際は、必ず `package.json` のバージョンをインクリメントする。

| 変更内容 | バンプ対象 | 例 |
|---|---|---|
| `.claude/` または実装ファイル（`.ts` 等）に変更あり | マイナー（パッチを 0 にリセット） | `0.5.3` → `0.6.0` |
| Markdown・コンテンツのみの変更 | パッチ | `0.5.3` → `0.5.4` |

複数種別が混在する場合は、より上位のルールを適用する（実装変更 > コンテンツのみ）。

## Git 使用ガイドライン

1. ファイルの作成・編集・削除は実行してよい
1. `git add`（ステージング）までは実行してよい
1. `git commit` は実行禁止（ユーザーが行う）
1. `git push` は実行禁止（ユーザーが行う）
1. `git fetch` や `git checkout` は必要に応じて実行してよい
